_target_: triforces.models.heads.EquivariantVectorHead
_partial_: true

# Equivariant node feature key produced by certain backbones/interactions.
equivariant_key: node_feats_equivariant
output_key: forces

# Gate MLP on invariant scalars
hidden_dims:
  - ${model.backbone.output_dim}
activation: silu
use_batch_norm: false
dropout: 0.0
